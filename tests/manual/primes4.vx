from file import stdin

def getdiv(a u64)
	b = a
	repeat
		d = 2UL
		repeat
			r = b % d
			if r == 0
				echo d
				b /= d
			else
				d++
			end
	brk brk b == 1
			echo "leave $b"
		until false
	until false
end

echon "Please enter a number: "
a = stdin.readul
getdiv a
