from file import stdin

def getdiv(a u64)
	b = a
	repeat
		d = 2UL
		repeat
			r = b % d
			if r == 0
				print d
				b /= d
			else
				d++
			end
	brk brk b == 1
			print "leave $b"
		until false
	until false
end

printn "Please enter a number: "
a = stdin.readul
getdiv a
