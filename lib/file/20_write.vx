cdecl fn fprintf(file voidptr, fmt cstring, ...) int
cdecl fn strlen(s cstring) size_t

pub inline fn File.write(s string)
	l = strlen(s)
	if fwrite(s, 1, l, this.voidptr) < l
		this.err = GetLastError
	end
end
